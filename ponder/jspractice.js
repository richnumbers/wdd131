const PI = 3.14159;
let radius = 5;

let area;

area = PI * radius * radius;
console.log("The area of the circle is: " + area);

radius = 20;
area = PI * radius * radius;
console.log("The area of the circle is: " + area);

const one = 1;
const two = '2';